version: '3.7'

services:
    db:
        image: mongo:3.6
        env_file: mongo.env
        ports:
        -   27017:27017

    api:
        # image: realsidsun/notes-api:latest
        build: .
        depends_on: 
            - db
        env_file: dev.env
        ports: 
            - 5050:5050